using System;

namespace ReverseProxy.Models
{
    public class Route
    {
        /*
         * TODO: create a route model for the destination service to match up against
         * needs to have at least two different types of route, static content and reverse proxy
         * static content is mostly for testing, but other reverse proxies also support this kind of thing.
         */
    }
}
